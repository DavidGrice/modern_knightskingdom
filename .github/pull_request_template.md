## What changed

<!-- One or two sentences. What now behaves differently, and why. -->

## Smoke tests

<!-- CI cannot run these (see CONTRIBUTING.md). Run them locally and say what
     happened, or say why none applied. Delete the line that does not fit. -->

- [ ] Ran locally: `node scripts/smokeNNN.mjs` → PASS
- [ ] Not applicable — no runtime behaviour changed (docs, config, types only)

## Checked

- [ ] `npm run verify` passes locally
- [ ] No asset, secret or `scripts/` file is being committed
