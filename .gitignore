# ---------------------------------------------------------------------------
# This repo is SOURCE ONLY. No secrets, no game assets, no design deliverables.
# Everything excluded here is either regenerable (`npm run prepare-assets`),
# private, or not mine to publish.
# ---------------------------------------------------------------------------

# --- build output & deps ---
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# --- SECRETS & USER DATA — never commit ---
# data/ holds session-secret, users.json and player saves
data/
.env
.env.*
*.pem
*.key
secrets.json

# --- extracted game assets (models, textures, sounds, rigs, animations) ---
# ~48 MB, all derived from the original 2000 title's resource files and
# regenerated by `npm run prepare-assets`. Not redistributable.
public/assets/
public/help/

# --- research material (manuals, LDraw dumps, OCR, set data) ---
kk_research_folder/

# --- UI design deliverables ---
# The src/styles/*.css and src/app/globals.css application stylesheets ARE
# committed; these are the handoff/design-pack artifacts, which are not.
Knights Kingdom UI.dc.html
Knights Kingdom UI - Brief.dc.html
/kk-tokens.css
/kk-icons.svg

# --- tooling & test harness ---
# Not published: the asset pipeline hardcodes local extraction paths, and
# scripts/shots/ is 222 MB of smoke-test screenshots. Both are local-only
# concerns. Note this means `npm run prepare-assets` cannot run from a fresh
# clone — see the README.
scripts/

# --- editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
